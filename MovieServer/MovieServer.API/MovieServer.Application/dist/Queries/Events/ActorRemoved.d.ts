import { IQuery } from "@Shared/Queries";
export default class ActorRemoved implements IQuery<void> {
    Id: string;
    ActorId: number;
    constructor(Id: string, ActorId: number);
}
//# sourceMappingURL=ActorRemoved.d.ts.map